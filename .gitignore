node_modules/
*.tgz
.claude/
docs/uat/
# Raw model streams behind docs/reviews/*.md. Big, and the markdown says everything.
docs/reviews/*.raw.jsonl
docs/reviews/*_raw.jsonl
docs/reviews/*_err.txt
# Generated by scripts/make-fixture.ts on pretest. Never commit anything session shaped here:
# an earlier commit held a real user transcript and needed a history rewrite to remove.
test/forked-session.jsonl
